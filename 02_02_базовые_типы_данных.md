# Базовые типы данных в Rust

## Введение

Система типов является одной из самых мощных особенностей Rust. Она обеспечивает безопасность памяти и предотвращает многие распространенные ошибки на этапе компиляции. Понимание типов данных в Rust является фундаментальным для эффективного использования языка.

В Rust все значения имеют определенный тип, который сообщает компилятору, как с ними работать. Rust — статически типизированный язык, что означает, что типы всех переменных должны быть известны на этапе компиляции. Компилятор обычно может вывести тип на основе значения и его использования, но иногда требуется явное указание типа.

В этой главе мы рассмотрим:
- Скалярные типы (целые числа, числа с плавающей точкой, логические значения, символы)
- Составные типы (кортежи, массивы)
- Преобразование типов
- Вывод типов и аннотации

## Скалярные типы

Скалярные типы представляют одиночные значения. Rust имеет четыре основных скалярных типа:

### Целочисленные типы

Целочисленные типы представляют числа без дробной части. Rust предоставляет несколько целочисленных типов различной разрядности и со знаком или без:

| Длина | Со знаком | Без знака |
|-------|-----------|-----------|
| 8-бит | `i8` | `u8` |
| 16-бит | `i16` | `u16` |
| 32-бит | `i32` | `u32` |
| 64-бит | `i64` | `u64` |
| 128-бит | `i128` | `u128` |
| архитектурно-зависимый | `isize` | `usize` |

Каждый тип имеет свой диапазон значений:
- Типы со знаком могут хранить числа от -(2^(n-1)) до 2^(n-1)-1, где n — количество бит
- Типы без знака могут хранить числа от 0 до 2^n-1

Например:
- `i8` может хранить значения от -128 до 127
- `u8` может хранить значения от 0 до 255

Типы `isize` и `usize` зависят от архитектуры компьютера: 64 бита на 64-битных системах и 32 бита на 32-битных системах. Они особенно полезны при индексации коллекций.

Целочисленные литералы можно записывать в различных форматах:

```rust
fn main() {
    // Десятичное число
    let decimal = 98_222;
    
    // Шестнадцатеричное число
    let hex = 0xff;
    
    // Восьмеричное число
    let octal = 0o77;
    
    // Двоичное число
    let binary = 0b1111_0000;
    
    // Байт (только u8)
    let byte = b'A'; // ASCII-код символа 'A'
    
    println!("Десятичное: {}", decimal);
    println!("Шестнадцатеричное: {}", hex);
    println!("Восьмеричное: {}", octal);
    println!("Двоичное: {}", binary);
    println!("Байт: {}", byte);
}
```

По умолчанию, если тип не указан явно и не может быть выведен из контекста, компилятор использует `i32` для целочисленных литералов.

### Типы с плавающей точкой

Rust имеет два типа с плавающей точкой: `f32` и `f64`, которые соответствуют стандарту IEEE-754:

- `f32`: 32-битное число с плавающей точкой одинарной точности
- `f64`: 64-битное число с плавающей точкой двойной точности (используется по умолчанию)

```rust
fn main() {
    let x = 2.0; // f64 по умолчанию
    let y: f32 = 3.0; // f32 с явным указанием типа
    
    println!("x: {}, y: {}", x, y);
    
    // Математические операции
    let sum = 5.0 + 10.0; // сложение
    let difference = 95.5 - 4.3; // вычитание
    let product = 4.0 * 30.0; // умножение
    let quotient = 56.7 / 32.2; // деление
    let remainder = 43.0 % 5.0; // остаток
    
    println!("Сумма: {}", sum);
    println!("Разность: {}", difference);
    println!("Произведение: {}", product);
    println!("Частное: {}", quotient);
    println!("Остаток: {}", remainder);
}
```

Числа с плавающей точкой соответствуют стандарту IEEE-754, что означает, что они могут представлять не только обычные числа, но и специальные значения, такие как бесконечность и NaN (Not a Number).

### Логический тип

Логический тип `bool` может иметь только два значения: `true` или `false`. Он занимает один байт в памяти.

```rust
fn main() {
    let t = true;
    let f: bool = false; // с явным указанием типа
    
    println!("t: {}, f: {}", t, f);
    
    // Логические операции
    let and = true && false; // логическое И
    let or = true || false; // логическое ИЛИ
    let not = !true; // логическое НЕ
    
    println!("true && false: {}", and);
    println!("true || false: {}", or);
    println!("!true: {}", not);
}
```

Логические значения часто используются в условных выражениях, таких как `if`:

```rust
fn main() {
    let number = 5;
    
    if number < 10 {
        println!("Число меньше 10");
    } else {
        println!("Число больше или равно 10");
    }
}
```

### Символьный тип

Тип `char` представляет одиночный символ Unicode и занимает 4 байта в памяти. Это позволяет представить не только ASCII-символы, но и символы из других языков, эмодзи и другие специальные символы.

Символьные литералы записываются в одинарных кавычках, в отличие от строковых литералов, которые записываются в двойных кавычках.

```rust
fn main() {
    let c = 'z';
    let z: char = 'ℤ'; // с явным указанием типа
    let heart_eyed_cat = '😻';
    
    println!("c: {}", c);
    println!("z: {}", z);
    println!("heart_eyed_cat: {}", heart_eyed_cat);
}
```

Поскольку `char` представляет символ Unicode, он может содержать гораздо больше значений, чем ASCII-символы. Это важно учитывать при работе с текстом на разных языках.

## Составные типы

Составные типы могут группировать несколько значений в один тип. Rust имеет два примитивных составных типа: кортежи и массивы.

### Кортежи (Tuples)

Кортеж — это способ группировки нескольких значений разных типов в одно составное значение. Кортежи имеют фиксированную длину: после объявления они не могут увеличиваться или уменьшаться.

```rust
fn main() {
    let tup: (i32, f64, u8) = (500, 6.4, 1);
    
    // Деструктуризация кортежа
    let (x, y, z) = tup;
    
    println!("Значения: x = {}, y = {}, z = {}", x, y, z);
    
    // Доступ к элементам кортежа через индекс
    let five_hundred = tup.0;
    let six_point_four = tup.1;
    let one = tup.2;
    
    println!("Через индексы: {} {} {}", five_hundred, six_point_four, one);
}
```

Кортеж без значений, `()`, является специальным типом, который имеет только одно значение, также записываемое как `()`. Этот тип называется единичным типом (unit type), а его значение — единичным значением (unit value). Выражения неявно возвращают единичное значение, если они не возвращают какое-либо другое значение.

### Массивы

Массив — это коллекция элементов одного типа с фиксированной длиной. В Rust массивы размещаются в стеке, а не в куче.

```rust
fn main() {
    // Массив с явным указанием типа и длины
    let a: [i32; 5] = [1, 2, 3, 4, 5];
    
    // Массив, где все элементы имеют одинаковое значение
    let b = [3; 5]; // Эквивалентно [3, 3, 3, 3, 3]
    
    println!("Первый массив: {:?}", a);
    println!("Второй массив: {:?}", b);
    
    // Доступ к элементам массива
    let first = a[0];
    let second = a[1];
    
    println!("Первый элемент: {}", first);
    println!("Второй элемент: {}", second);
}
```

Попытка доступа к элементу массива за его пределами приведет к панике во время выполнения:

```rust
fn main() {
    let a = [1, 2, 3, 4, 5];
    
    // Следующая строка вызовет панику во время выполнения
    // let element = a[10]; // index out of bounds: the len is 5 but the index is 10
}
```

Это одна из особенностей безопасности Rust: компилятор гарантирует, что ссылки на массив всегда действительны.

## Преобразование типов

Rust требует явного преобразования типов в большинстве случаев. Это делается с помощью ключевого слова `as`.

```rust
fn main() {
    let a = 5;
    let b = 2.0;
    
    // Явное преобразование
    let c = a as f64 + b;
    
    println!("c = {}", c);
    
    // Преобразование символа в число
    let d = 'A' as u8;
    println!("ASCII-код символа 'A': {}", d);
    
    // Преобразование числа в символ
    let e = 66 as char;
    println!("Символ с ASCII-кодом 66: {}", e);
}
```

Важно отметить, что преобразование между некоторыми типами может привести к потере данных или неожиданным результатам:

```rust
fn main() {
    let x: u8 = 255;
    
    // Преобразование в u8 с потерей данных
    let y = 256 as u8; // Результат будет 0 из-за переполнения
    
    println!("x: {}, y: {}", x, y);
}
```

## Вывод типов и аннотации

Rust имеет мощную систему вывода типов, которая часто может определить тип переменной на основе её значения и использования. Однако иногда необходимо явно указать тип:

```rust
fn main() {
    // Компилятор выводит тип
    let guess = "42".parse().expect("Не число!");
    // Ошибка: type annotations needed
    
    // Явное указание типа
    let guess: u32 = "42".parse().expect("Не число!");
    println!("guess: {}", guess);
}
```

Аннотации типов особенно полезны в следующих случаях:
1. Когда компилятор не может однозначно вывести тип
2. Для улучшения читаемости кода
3. При работе с обобщенными типами

## Примеры использования различных типов

### Пример 1: Расчет площади прямоугольника

```rust
fn main() {
    // Используем кортеж для представления прямоугольника
    let rect: (u32, u32) = (30, 50);
    
    // Используем деструктуризацию для получения ширины и высоты
    let (width, height) = rect;
    
    let area = width * height;
    
    println!("Площадь прямоугольника: {} квадратных единиц", area);
}
```

### Пример 2: Работа с температурой

```rust
fn main() {
    // Температура в градусах Цельсия
    let celsius: f64 = 23.0;
    
    // Преобразование в градусы Фаренгейта
    let fahrenheit = celsius * 9.0 / 5.0 + 32.0;
    
    println!("{}°C = {}°F", celsius, fahrenheit);
    
    // Преобразование обратно в градусы Цельсия
    let celsius_again = (fahrenheit - 32.0) * 5.0 / 9.0;
    
    println!("{}°F = {}°C", fahrenheit, celsius_again);
}
```

### Пример 3: Работа с символами и строками

```rust
fn main() {
    // Массив символов
    let vowels: [char; 5] = ['a', 'e', 'i', 'o', 'u'];
    
    // Проверка, является ли символ гласной
    let c = 'e';
    let is_vowel = vowels.contains(&c);
    
    println!("Символ '{}' является гласной: {}", c, is_vowel);
    
    // Преобразование символа в верхний регистр
    let uppercase = (c as u8 - 32) as char;
    println!("Символ '{}' в верхнем регистре: '{}'", c, uppercase);
}
```

## Лучшие практики

1. **Выбирайте подходящий тип**: Используйте наиболее подходящий тип для ваших данных. Например, для небольших целых чисел используйте `i32` или `u32`, а не `i64` или `u64`, если не требуется больший диапазон.

2. **Будьте осторожны с преобразованием типов**: Явно преобразуйте типы, когда это необходимо, но будьте осторожны с потенциальной потерей данных.

3. **Используйте аннотации типов для ясности**: Даже если компилятор может вывести тип, иногда полезно явно указать его для улучшения читаемости кода.

4. **Избегайте переполнения**: Будьте внимательны при работе с числовыми типами, особенно при выполнении арифметических операций, которые могут привести к переполнению.

5. **Используйте составные типы для группировки связанных данных**: Кортежи и массивы полезны для группировки связанных данных, но для более сложных структур данных рассмотрите возможность использования структур (structs) и перечислений (enums).

## Упражнения

1. **Работа с целыми числами**:
   Напишите программу, которая вычисляет сумму, разность, произведение и частное двух целых чисел. Обработайте возможное переполнение и деление на ноль.

2. **Преобразование типов**:
   Создайте программу, которая преобразует температуру из градусов Цельсия в градусы Фаренгейта и Кельвины. Используйте соответствующие типы данных и явное преобразование типов.

3. **Работа с массивами**:
   Напишите программу, которая находит минимальное, максимальное и среднее значение в массиве целых чисел.

4. **Кортежи и деструктуризация**:
   Создайте функцию, которая принимает кортеж с информацией о студенте (имя, возраст, средний балл) и выводит эту информацию в форматированном виде.

## Заключение

Понимание базовых типов данных в Rust является фундаментальным для эффективного использования языка. Скалярные типы (целые числа, числа с плавающей точкой, логические значения, символы) и составные типы (кортежи, массивы) предоставляют основу для представления данных в программах на Rust.

В следующей главе мы рассмотрим комментарии и документацию в Rust, что поможет сделать ваш код более понятным и поддерживаемым.